import { HotkeysWrapper } from './TagTree';

const DEFAULT_TAG_NAME = 'New Tag';
const DEFAULT_COLLECTION_NAME = 'New Collection';

export default HotkeysWrapper;
export { DEFAULT_COLLECTION_NAME, DEFAULT_TAG_NAME };
